import React from 'react'

const Button = () => {
  return (
    <button className='w-full bg-sky-600 p-4 rounded-md text-white font-semibold md:w-[50vw]'>
pay now
    </button>
  )
}

export default Button